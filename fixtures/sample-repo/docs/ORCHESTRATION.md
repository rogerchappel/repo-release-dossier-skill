# Orchestration

The fixture is used in read-only mode by the test suite and smoke command. It
should never be pushed, tagged, published, or used as a real release target.
Agents can inspect it to verify markdown rendering and evidence scoring.
